/**
 * The Hollow — the firelit room the games are played in.
 *
 * The cave is the whole section now. There is no meadow above it and no
 * descent into it: you swipe here and you are in the room, which is what the
 * place was always trying to be.
 */

import Cave from '@/world/cave/Cave'

export default function Hollow() {
  return <Cave />
}
